import { StorefrontFlyerSectionInput } from "graphql-utils";

export type FlyerSectionProps = {
  pageIndex: number;
  sectionIndex: number;
  sectionInput: StorefrontFlyerSectionInput;
};

export default function FlyerSection({}: FlyerSectionProps) {
  // TODO: Implement the flyer section component. This will represent a section within a flyer page, and will hold the products that are added to that section.
  // This will have a form that will allow the user to add a title (optional), and a description (optional) to the section. The user will also be able to add products to the section, and reorder them within the section (This will be implemented later so don't worry about this at the moment).
  // And finally a hero image upload section. Make sure to display the image when the user selects an image.
  return <></>
}
